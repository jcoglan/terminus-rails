== Terminus Rails example

This is a minimal example of running Terminus on a Rails 3 application. To get
up and running:

    bundle install
    bundle exec terminus
    open http://127.0.0.1:7004/
    bundle exec rspec spec/

See files in spec/ for examples.
